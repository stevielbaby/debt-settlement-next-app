// import { sql } from '@/app/lib/db'; // Temporarily disabled during database unification

async function checkOperator() {
  console.log('Operator check temporarily disabled during database unification');
  console.log('Database access has been unified to Prisma-only');
}

checkOperator();
